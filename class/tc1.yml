parameters:
  kapitan:
    compile:
      - input_paths:
          - tc1/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - tc1/component/main.jsonnet
        input_type: jsonnet
        output_path: tc1/

